info:
  name: Preview KTM
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/users/4/ktm"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
