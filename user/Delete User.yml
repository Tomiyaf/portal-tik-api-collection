info:
  name: Delete User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/users/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
