info:
  name: Delete User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/users/4"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
