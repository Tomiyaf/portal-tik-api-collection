info:
  name: Update Password
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/profile/update-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
