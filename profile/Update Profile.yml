info:
  name: Update Profile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
