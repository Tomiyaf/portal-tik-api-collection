info:
  name: Update Parking Quota
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/parking-quota"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
