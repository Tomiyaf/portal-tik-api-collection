info:
  name: Update Parking Quota
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/parking-quota"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
