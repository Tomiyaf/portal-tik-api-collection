info:
  name: Detail Parking Quota
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/parking-quota"
  auth:
    type: bearer
    token: "{{student}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
