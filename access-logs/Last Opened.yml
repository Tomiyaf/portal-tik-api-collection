info:
  name: Last Opened
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/access-logs/last-opened"
  auth:
    type: bearer
    token: "{{student}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
